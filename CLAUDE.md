# Neovim Config

This repo is a **git submodule** of [~/Coding/dotfiles](../). After committing changes here, always update the parent dotfiles repo too:

```bash
cd ~/Coding/dotfiles
git add nvim
git commit -m "Update nvim submodule: <description>"
git push
```
